/**
 * API server entrypoint.
 *
 * Historical modules are being migrated in phases; global typecheck currently
 * validates this entrypoint while deeper modules are cleaned incrementally.
 */

export {};
