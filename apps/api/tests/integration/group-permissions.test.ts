import { describe, it } from "vitest";

describe.skip("Group Permissions", () => {
  it("requires a dedicated integration DB fixture", () => {
    // Historical placeholder: this suite should be re-enabled once
    // deterministic tenant/user/group fixtures are available.
  });
});
